
apiVersion: v1
kind: Service
metadata:
  name: project-2-service
  namespace: default
spec:
  selector:
    app: project-2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30008
 
